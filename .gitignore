*.~*
*.py[cod]
*.pyo
*.pyd
*dump*

# Python caches / test output
__pycache__/
**/__pycache__/
.pytest_cache/
**/.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
*.coverage
coverage.xml
htmlcov/

# Virtual environments (never commit)
.venv/
venv/
env/
ENV/

# Packaging / local installs
*.egg-info/
.eggs/
dist/
build/
pip-wheel-metadata/

# Local app/runtime state
boomer.ini
logs/
temp/
bookmarks.json
.DS_Store

# Autosave / recovery snapshots if app data is redirected into the repo
*.pkl
*.pkl.tmp
*.json.tmp
autosave/
recovery/
working_copies/
working-copies/
Boomer/
BoomerPySide6/

# Generated exports / reports
*_edited.csv
*_cleaned.csv
*_cleaned.xlsx
merge_top_*.csv
merge_bot_*.csv
merged*.csv
merged*.xlsx
report*.html
examples/*/*.html
# App-generated cross-check HTML (any prefix / BOM naming)
*cross_check_report*.html

# Local UI screenshots added by mistake (keep repo lean)
examples/t-olp screenshots/

# Local-only example/archive folders
examples/SmoCharger/
examples2/
example_ctk/

# Local-only AI / agent context (not shipped in git)
LLM.md
session-ses_*.md

# IDE / editor (local only; keep shared .vscode in repo if present)
.idea/
.cursor/

# Optional vendored dependency experiments
src/odfpy

# Keep the shared example/user component database tracked.
!components.txt
